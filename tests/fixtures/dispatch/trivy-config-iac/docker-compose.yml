version: "3.8"
services:
  web:
    image: nginx:1.25
    ports:
      - "8080:80"
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: example
  sidecar:
    image: alpine:3.18
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
